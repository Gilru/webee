<div class="<%= yield(:background) %>">
  <nav class="navbar navbar-default back-bord-remove" role="navigation">
    <div class="container back-bord-remove">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <i class="ti-unlock <%= yield(:toggle_nav) %> login-padlock-size"></i>
          </button>

          <ul class="nav navbar-nav">
            <li class="dropdown">
              <a href="#" class="dropdown-toggle navbar-brand color-title" data-toggle="dropdown">
                <i class="ti-align-justify <%= yield(:bars) %>"></i>
                <span class="<%= yield(:bars) %> <%= yield(:visibility_brand_info) %>">Webee</span>
              </a>
              <ul class="dropdown-menu background-white" role="menu">
                <li class="center style-x">
                  <i class="ti-close <%= yield(:border_cross_menu_color) %> <%= yield(:icon_color) %>"></i></li>
                <li><%= link_to "home", root_path, class: "hover-home size-menu light" %></li>
                <li class="divider"></li>
                <li><%= link_to "How Does It Work", how_does_it_work_path, class: "hover-how-it-works size-menu light" %></li>
                <li class="divider"></li>
                <li><%= link_to "portofolio", info_portofolio_path, class: "hover-portofolio size-menu light" %></li>
                <li class="divider"></li>
                <li><%= link_to "Pricing", info_pricing_path, class: "hover-pricing size-menu light" %></li>
                <li class="divider"></li>
                <li><%= link_to "why us", info_why_us_path, class: "hover-why-us size-menu light" %>
                <li class="divider"></li>
                <li><%= link_to "blog", blogs_path %></li>
                <li class="divider"></li>
                <li><%= link_to "website", websites_path %></li>
                <% if user_signed_in? && current_user.manager? %>
                    <li class="divider"></li>
                    <li><%= link_to "user info", users_index_path %></li>
                <% end %>
                <li class="divider"></li>
                <li class="center ic-menu-size"><i class="ti-twitter text-info"></i>
                  <i class="ti-facebook text-primary"></i>
                  <i class="ti-google text-danger"></i>
                  <i class="ti-linkedin color-linkedin-ic"></i>
                </li>


              </ul>
            </li>
          </ul>


        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">


          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
                <li><%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-primary btn-outline btn-rounded light #{yield(:signout)}" %></li>
            <% else %>
                <li><%= link_to "Sign in", new_user_session_path, class: "btn btn-primary btn-outline btn-rounded light #{yield(:signin)}" %></li>
            <% end %>

          </ul>
        </div>
        <!-- /.navbar-collapse -->
      </div>
      <!-- /.container-fluid -->
    </div>
  </nav>
</div>