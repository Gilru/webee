<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <i class="ti-unlock asowesome-color login-padlock-size"></i>
        </button>

        <ul class="nav navbar-nav">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle navbar-brand color-title" data-toggle="dropdown">
              <i class="ti-align-justify"></i> <span class="light">Webee</span>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li>
              <li class="center style-x"><i class="ti-close border-cross-menu pink"></i></li>
              <li><%= link_to "home", root_path, class: "hover-home size-menu light" %></li>
              <li class="divider"></li>
              <li><%= link_to "How Does It Work", how_does_it_work_path, class: "hover-how-it-works size-menu light" %></li>
              <li class="divider"></li>
              <li><%= link_to "portofolio", info_portofolio_path, class: "hover-portofolio size-menu light" %></li>
              <li class="divider"></li>
              <li><%= link_to "Pricing", info_pricing_path, class: "hover-pricing size-menu light" %></li>
              <li class="divider"></li>
              <li><%= link_to "why us", info_why_us_path, class: "hover-why-us size-menu light" %>
              <li class="divider"></li>
              <li><%= link_to "blog", blogs_path %></li>

              <% if user_signed_in? && current_user.manager? %>
                  <li class="divider"></li>
                  <li><%= link_to "user info", users_index_path %></li>
              <% end %>

              <% if user_signed_in? %>
                  <li class="divider"></li>
                  <li><%= link_to "project", projects_path %></li>
              <% end %>
              <li class="divider"></li>
              <li><%= link_to "website", websites_path %></li>

              <% if user_signed_in? && current_user.admin? %>
                  <li class="divider"></li>
                  <li><%= link_to "manage website", seller_path %></li>
              <% end %>
              <li class="divider"></li>
              <li class="center ic-menu-size"><i class="ti-twitter text-info"></i>
                  <i class="ti-facebook text-primary"></i>
                  <i class="ti-google text-danger"></i>
                  <i class="ti-linkedin color-linkedin-ic"></i>
              </li>


            </ul>
          </li>
        </ul>

        <!--=======================here=================-->

        <!--form search beginning-->
        <%= form_tag search_blogs_path, class: "navbar-form navbar-right margin-form
        #{yield(:visibility_search_blog).presence || 'hidden'}", method: :get do %>
            <p>
              <%= text_field_tag :search, params[:search], placeholder: "Search", class: "field__input" %>

            </p>
        <% end %>

        <!--form search-->


        <!--======end=================-->

        <!--=======================here=================-->

        <!--form search beginning-->
        <%= form_tag search_websites_path, class: "navbar-form navbar-right margin-form
         #{yield(:visibility_search_website).presence || 'hidden'}", method: :get do %>
            <p>
              <%= text_field_tag :search, params[:search], placeholder: "Search", class: "field__input" %>

            </p>
        <% end %>

        <!--form search-->

        <!--======end=================-->

        <!--=======================here=================-->

        <form class="navbar-form navbar-right margin-form hidden" role="search">
          <div class="form-group">
            <input type="text" class="form-control" placeholder="Search">
          </div>
          <button type="submit" class="btn btn-default">Submit</button>
        </form>

        <!--======end=================-->


      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

        <div class="btn-group margin-browse-button <%= yield(:browse_website_visibility).presence || 'hidden' %>">
          <button type="button" class="btn btn-info">Browse by Field</button>
          <button type="button" class="btn btn-info dropdown-toggle" data-toggle="dropdown">
            <span class="caret"></span>
            <span class="sr-only">Toggle Dropdown</span>
          </button>
          <ul class="dropdown-menu" role="menu">
            <% Category.all.each do |category| %>

                <li><%= link_to category.name, websites_path(category: category.name) %></li>
            <% end %>


          </ul>
        </div>

        <%= link_to blogs_path,
                    class: "margin-menu-back navbar-nav text-primary
                light #{yield(:go_back_blog_visibility).presence || 'hidden'}" do %>
            <i class="ti-arrow-circle-left"></i> Go Back To Blogs

        <% end %>

        <%= link_to profile_path,
                    class: "margin-menu-back navbar-nav text-primary
                light #{yield(:go_back_profile_visibility).presence || 'hidden'}" do %>
            <i class="ti-arrow-circle-left"></i> Go Back To My Profile

        <% end %>


        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
              <li><%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-primary btn-outline btn-rounded light border-color-degr btn-lg" %></li>
          <% else %>
              <li><%= link_to "Sign in", new_user_session_path, class: "btn btn-primary btn-outline btn-rounded light border-color-degr btn-lg" %></li>
          <% end %>

        </ul>
      </div>
      <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
  </div>
</nav>