<div id="page-content-wrapper">
  <div class="container-fluid">

    <div class="row">
      <div class="col-md-1"></div>
      <div class="col-md-10">

        <h1 class="thin">New order</h1>
        <h2 class="thin">name: <%= @website.name %> ; price: <%= number_to_currency(@website.price) %></h2>

        <%= render 'form' %>

        <%= link_to 'Back', website_path(@website) %>

      </div>
      <div class="col-md-1"></div>
    </div>

  </div>
</div>